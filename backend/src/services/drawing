Frontend
   ↓
download.routes.js
   ↓
download.controller.js
   ↓
downloader.service.js  ← THIS FILE
   ↓
yt-dlp + FFmpeg
   ↓
final MP4/MP3



You click Download
        ↓
POST /api/download
        ↓
downloadController
        ↓
download(url, type)
        ↓
yt-dlp
        ↓
video + audio
        ↓
FFmpeg merges them
        ↓
MP4 saved in downloads/
        ↓
controller returns:
{
  message: "Download completed",
  fileName: "...mp4",
  downloadUrl: "/downloads/...mp4"
}

Browser
   │
   │ POST /api/download
   ↓
Backend
   │
   │ starts yt-dlp
   ↓
yt-dlp downloads
   │
   │ waits...
   ↓
Backend
   │
   │ "Download completed"
   ↓
Browser

PROGRESS

Browser
   │
   │ "Start this download"
   ↓
Backend
   │
   │ starts yt-dlp
   ↓
yt-dlp
   │
   ├── 10% ──────→ Browser
   ├── 27% ──────→ Browser
   ├── 51% ──────→ Browser
   ├── 78% ──────→ Browser
   └── 100% ─────→ Browser

                       ┌──────────────┐
                    │   Browser    │
                    └──────┬───────┘
                           │
                     POST /download
                           │
                           ▼
                    ┌──────────────┐
                    │   Express    │
                    └──────┬───────┘
                           │
                           ▼
                       yt-dlp
                           │
                    43.2%, 57.8%...
                           │
                           ▼
                    ┌──────────────┐
                    │    Node.js   │
                    └──────┬───────┘
                           │
                       SSE stream
                           │
                           ▼
                    ┌──────────────┐
                    │   Browser    │
                    │   57.8%      │
                    └──────────────┘
                    checkProgress(id)
       ↓
get progress
       ↓
if progress < 100
       ↓
check again after 1 second
       ↓
if progress === 100
       ↓
STOP

User clicks Download
        ↓
POST /api/download
        ↓
Download starts
        ↓
yt-dlp creates file
        ↓
Server detects completed file
        ↓
Server gives frontend file URL
        ↓
User clicks/downloads file

Frontend
   ↓
POST /api/download
   ↓
Controller
   ↓
Downloader service
   ↓
yt-dlp
   ↓
File created
   ↓
downloadStore
   ↓
GET /api/download/file/:downloadId
   ↓
File information returned ✅

downloader.service.js
    → downloads the file
    → remembers the filename

download.controller.js
    → receives the request
    → finds the file
    → sends the file to the browser

    YouTube URL
    ↓
POST /api/download
    ↓
Controller validates URL + type
    ↓
Downloader service
    ↓
yt-dlp downloads the media
    ↓
downloadStore remembers the file
    ↓
GET /api/download/file/:downloadId
    ↓
res.download()
    ↓
Browser downloads the MP3
    ↓
User opens it → plays ✅